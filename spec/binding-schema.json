{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://gtme.spec/binding-schema.json",
  "title": "Declarative binding",
  "description": "A tier-1 adapter: a YAML document the runner's generic HTTP engine interprets deterministically (SPEC.md §10a, ADR-022). Validate after decoding the YAML to JSON. Deliberately ~8 primitives; the moment a binding needs logic it graduates to a process adapter — no expression language may ever grow in here. Templating (ADR-057, M31): a leaf is a Liquid object in gtme's one bounded dialect over 'record.<field>', 'config.<key>', 'variables.<name>' and 'session' — objects only, no block tags, the fixed filter allowlist (SPEC.md §10 item 10; 'default' is the fallback: '{{ record.linkedin_url | default: record.linkedin_internal_url }}'). A leaf that is exactly one placeholder substitutes the typed value; leaves resolving to empty/null are omitted from the request (omitempty). The retired '{{a|b}}' alternatives fail verify naming the rewrite. In a deliver body, an object may carry the key '$variables': true — the resolved variables: mapping (minus any variable referenced individually elsewhere in the body) is spliced into that object. The engine pre-fills config defaults declared in config_schema.",
  "type": "object",
  "required": [
    "id",
    "version",
    "role",
    "entity_type",
    "request"
  ],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "Adapter id, e.g. 'attio/assert'. Named by the contract owner (ADR-026). The id's vendor prefix is the COST provider name."
    },
    "version": {
      "type": "integer",
      "minimum": 1
    },
    "role": {
      "enum": [
        "source",
        "traverse",
        "enrich",
        "deliver"
      ],
      "description": "source: pagination + cursor/STATE, config 'limit' caps emitted records; traverse (ADR-054): a source-shaped request templated per parent from '{{record.<field>}}' placeholders (its needs), emitting records of entity_type related to the parent by 'relation', 'limit' capping children per parent; enrich: per-record request; deliver: idempotency + dry-run receipts."
    },
    "entity_type": {
      "type": "string",
      "minLength": 1,
      "description": "A type file's name (SPEC.md §4a). For a traverse this is the OUTPUT type; 'from' is the input. A binding MAY ship the type it emits as types/<name>.json beside binding.yaml (never an embedded name)."
    },
    "from": {
      "type": "string",
      "minLength": 1,
      "description": "Traverse only (ADR-054): the input type; the planner requires it to equal the current segment's type."
    },
    "relation": {
      "type": "object",
      "required": [
        "name",
        "from"
      ],
      "additionalProperties": false,
      "description": "Traverse only (ADR-054): the edge written between every emitted record and its parent, and which end it starts at (record: authored_by from the post to its author; parent: works_at from the person to the company).",
      "properties": {
        "name": {
          "type": "string",
          "pattern": "^[a-z][a-z0-9_]*$"
        },
        "from": {
          "enum": [
            "record",
            "parent"
          ]
        }
      }
    },
    "needs": {
      "description": "Same manifest surface as process adapters (SPEC.md §6): a JSON Schema, 'dynamic', or the dynamic-with-floor object form."
    },
    "provides": {
      "description": "Same manifest surface as process adapters (SPEC.md §6)."
    },
    "config_schema": {
      "type": "object"
    },
    "freshness_days": {
      "type": "integer",
      "minimum": 0
    },
    "cost_estimate_usd": {
      "type": "number",
      "minimum": 0
    },
    "keep_payloads": {
      "type": "boolean",
      "description": "ADR-030: whether the engine attaches raw responses to this binding's RECORDs for retention. Default true; steps may override with keep_payloads: false in config."
    },
    "payload_ttl_days": {
      "type": "integer",
      "minimum": 0,
      "description": "ADR-030: retention window for this binding's payloads (default 90). 0 = no expiry."
    },
    "credentials": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "credentials_optional": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "auth": {
      "type": "object",
      "required": [
        "type",
        "env"
      ],
      "additionalProperties": false,
      "properties": {
        "type": {
          "enum": [
            "header",
            "query",
            "basic",
            "bearer"
          ]
        },
        "name": {
          "type": "string",
          "description": "Header or query-param name; unused for basic/bearer."
        },
        "env": {
          "type": "string",
          "description": "Env var holding the credential (resolved per SPEC.md §6 credentials rules)."
        },
        "prefix": {
          "type": "string",
          "description": "Optional literal prefix for the value, e.g. 'Bearer '."
        }
      }
    },
    "request": {
      "type": "object",
      "required": [
        "method",
        "url"
      ],
      "additionalProperties": false,
      "description": "Templating draws only from step config, the record's canonical fields, resolved variables, and the session id — no expressions, no computation.",
      "properties": {
        "method": {
          "enum": [
            "GET",
            "POST",
            "PUT",
            "PATCH",
            "DELETE"
          ]
        },
        "url": {
          "type": "string",
          "minLength": 1
        },
        "query": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "description": "Query-param templates; empty resolutions are omitted."
        },
        "headers": {
          "type": "object",
          "additionalProperties": {
            "type": "string"
          }
        },
        "body": {
          "description": "Body template: a JSON shape whose string leaves are templates. Empty resolutions are omitted. '$variables': true in an object splices the resolved variables map."
        }
      }
    },
    "pagination": {
      "type": "object",
      "required": [
        "strategy"
      ],
      "additionalProperties": false,
      "properties": {
        "strategy": {
          "enum": [
            "page",
            "cursor",
            "offset"
          ]
        },
        "param": {
          "type": "string",
          "description": "Request param carrying the page number / cursor / offset."
        },
        "in": {
          "enum": [
            "query",
            "body"
          ],
          "description": "Where pagination params ride (default query)."
        },
        "cursor_path": {
          "type": "string",
          "description": "Dotted path to the next cursor in the response (cursor strategy)."
        },
        "page_size": {
          "description": "Records per page: an integer or a template (e.g. '{{config.per_page}}').",
          "anyOf": [
            {
              "type": "integer",
              "minimum": 1
            },
            {
              "type": "string"
            }
          ]
        },
        "size_param": {
          "type": "string",
          "description": "Request param carrying the page size."
        },
        "termination": {
          "type": "object",
          "additionalProperties": false,
          "properties": {
            "empty_records": {
              "type": "boolean",
              "description": "Stop when a page yields zero records (engine default: always on)."
            },
            "short_page": {
              "type": "boolean",
              "description": "Stop when a page yields fewer records than page_size."
            },
            "total_pages_path": {
              "type": "string",
              "description": "Dotted path to a total-page count; alternatives via '|'."
            }
          }
        },
        "max": {
          "type": "integer",
          "minimum": 1,
          "description": "Hard cap on pages fetched."
        }
      }
    },
    "extract": {
      "type": "object",
      "required": [
        "records",
        "fields"
      ],
      "additionalProperties": false,
      "properties": {
        "records": {
          "description": "Dotted path to the record array (or single record) in the response; a list means first non-empty wins.",
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "array",
              "items": {
                "type": "string"
              },
              "minItems": 1
            }
          ]
        },
        "fields": {
          "type": "object",
          "minProperties": 1,
          "description": "canonical (or vendor-namespaced) field name -> extraction rule.",
          "additionalProperties": {
            "oneOf": [
              {
                "type": "string",
                "description": "Dotted path within a record."
              },
              {
                "type": "object",
                "additionalProperties": false,
                "properties": {
                  "path": {
                    "type": "string"
                  },
                  "paths": {
                    "type": "array",
                    "items": {
                      "type": "string"
                    },
                    "minItems": 1,
                    "description": "Alternatives; first non-empty wins."
                  },
                  "transform": {
                    "type": "string",
                    "description": "A registry normalization rule id (SPEC.md §4a) — or 'linkedin', the engine-owned §4 classify-and-route rule that stores a LinkedIn-URL-shaped value under the field matching its shape. Never arbitrary logic."
                  },
                  "absent": {
                    "type": "array",
                    "items": {},
                    "description": "Sentinel values the vendor uses to mean 'no value' (e.g. a locked-email placeholder, 0 for an unknown count); matched exactly, treated as absent."
                  },
                  "skip_if_input": {
                    "type": "boolean",
                    "description": "Omit this output field when the projected input already carried a non-empty value for it (e.g. only emit a resolved public linkedin_url when the record arrived without one)."
                  }
                }
              }
            ]
          }
        }
      }
    },
    "errors": {
      "type": "object",
      "description": "HTTP status (or status class like '4xx') -> verdict mapping; statuses the map does not name follow the engine's default classification (SPEC.md §8 exit-code classes).",
      "additionalProperties": {
        "type": "object",
        "required": [
          "verdict"
        ],
        "additionalProperties": false,
        "properties": {
          "verdict": {
            "enum": [
              "fail_record",
              "fail_run",
              "retry",
              "skip"
            ]
          },
          "reason": {
            "type": "string"
          }
        }
      }
    },
    "idempotency": {
      "enum": [
        "native",
        "ledger"
      ],
      "description": "Which party guarantees delivery dedupe: the vendor's own semantics (native, e.g. an assert/upsert endpoint) or the runner's deliveries table (ledger). Required for role: deliver."
    },
    "idempotency_scope": {
      "type": "string",
      "description": "Deliver bindings (ADR-044): config key whose resolved value scopes deliveries rows (SPEC §8). Optional."
    },
    "cost": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "per": {
          "enum": [
            "record",
            "request",
            "unit"
          ]
        },
        "amount_usd": {
          "description": "Dollars per unit of `per`: a number, or a template resolved from config (e.g. '{{config.cost_per_record_usd}}') for vendors whose price is plan-dependent (ADR-046). Declare the knob in config_schema. An unresolved template costs $0 at run time and `gtme plan` prints `est/record: unset`. A page-billed endpoint declares `per: request` — `per: record` counts emitted records, and a `limit` truncates emission after the vendor has billed the page.",
          "anyOf": [
            {
              "type": "number",
              "minimum": 0
            },
            {
              "type": "string",
              "pattern": "^\\s*\\{\\{[^{}]+\\}\\}\\s*$"
            }
          ]
        },
        "unit_path": {
          "type": "string",
          "description": "Dotted path to a units-consumed count in the response (per: unit)."
        }
      }
    },
    "retry": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "max_attempts": {
          "type": "integer",
          "minimum": 0
        },
        "backoff_seconds": {
          "type": "number",
          "minimum": 0
        },
        "rate_per_hour": {
          "type": "integer",
          "minimum": 1
        },
        "windows": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Hourly send/fetch windows, e.g. '09:00-17:00'."
        }
      }
    },
    "session": {
      "type": "object",
      "additionalProperties": false,
      "description": "Optional pagination-consistency session: a UUID minted per run and passed through on every request.",
      "properties": {
        "param": {
          "type": "string"
        },
        "in": {
          "enum": [
            "header",
            "query",
            "body"
          ]
        }
      }
    }
  }
}
