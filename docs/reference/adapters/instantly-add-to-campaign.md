---
# generated by make docs-reference (cmd/docsgen); do not edit, fix the source and regenerate
name: "instantly/add-to-campaign"
description: "Built-in deliver adapter for person records: its manifest, needs, provides, config keys, cost, and an example step"
for: "You're about to put `instantly/add-to-campaign` in a step and want its keys, fields, credentials, and cost before plan tells you."
learn:
  - "what the adapter needs and provides"
  - "every `with:` key it accepts"
  - "what it costs and which credential it demands"
roles: [builder, extender, agent]
generated_by: "make docs-reference (cmd/docsgen): gtme help --agent, docs/_adapters.json, spec/fields/*.json, spec/ledger.sql, and each concept page's defines:. Do not edit; fix the source and regenerate."
links:
  - to: /concepts/adapter-tiers
    type: relates-to
    description: "Defines adapter and manifest"
  - to: /concepts/gate-ladder
    type: relates-to
    description: "Defines plan and preflight"
  - to: /concepts/ledger
    type: relates-to
    description: "Defines ledger"
  - to: /concepts/pipeline
    type: relates-to
    description: "Defines step"
  - to: /concepts/runs-and-receipts
    type: relates-to
    description: "Defines delivery and run"
  - to: /concepts/steps-and-roles
    type: relates-to
    description: "Defines compose, deliver, review, and role"
  - to: /concepts/types-and-traverse
    type: relates-to
    description: "Defines type"
---

# instantly/add-to-campaign

A built-in deliver adapter for person records: it needs the step's `uses:` fields, `email` and declares no fields of its own. Plan prices it at $0 per record, and it demands `INSTANTLY_API_KEY`.

## Manifest

| Key | Value |
|---|---|
| id | `instantly/add-to-campaign` |
| version | 1 |
| role | deliver |
| entity type | person |
| credentials | `INSTANTLY_API_KEY` |
| cost estimate per record | $0 |
| attests | yes: a delivery becomes `sent` when the vendor confirms it |

## Needs

```json
{
  "dynamic": true,
  "properties": {
    "email": {
      "type": "string"
    }
  },
  "required": [
    "email"
  ],
  "type": "object"
}
```

## Provides

Nothing; a delivery row is what it writes.

## Config keys

The keys `with:` accepts. Plan rejects any other key.

| Key | Type | Required | What it sets |
|---|---|---|---|
| `base_url` | string |  | Override the API host (testing) |
| `campaign` | string | yes | Campaign name (resolved to an id once per run) or the campaign id itself |
| `preflight` | boolean |  | ADR-040: check the live campaign before sending (default true); false skips |
| `skip_if_in_campaign` | boolean |  | Ask Instantly to ignore leads already in the campaign (default true) |
| `variables` | object |  | Egress mapping, target merge-field name → ledger field (ADR-018). Injected by the runner from the step-level variables: key — never authored inside with:. |

## Example

From [See it run](/start/show-me):

```yaml
  - id: send
    use: instantly/add-to-campaign
    with: { campaign: "Q3 VP Marketing" }
    variables:
      first_name: first_name
      first_line: first_line
    idempotency: email
```

## Used in

- The `full-funnel-with-uses` example in `gtme help --agent`
- The `human-review-then-cron` example in `gtme help --agent`
- The `csv-sourced-compose-with-uses` example in `gtme help --agent`
