---
# generated by make docs-reference (cmd/docsgen); do not edit, fix the source and regenerate
name: "costs"
description: "Spend per step/record"
for: "You're writing SQL that reads `costs` and need its columns, what each holds, and the DDL as the spec states it."
learn:
  - "every column and its type"
  - "what the object holds and who writes it"
  - "the DDL as the spec states it"
roles: [operator, builder, agent]
generated_by: "make docs-reference (cmd/docsgen): gtme help --agent, docs/_adapters.json, spec/fields/*.json, spec/ledger.sql, and each concept page's defines:. Do not edit; fix the source and regenerate."
links:
  - to: /concepts/ledger
    type: relates-to
    description: "Defines ledger"
  - to: /concepts/pipeline
    type: relates-to
    description: "Defines step"
  - to: /concepts/types-and-traverse
    type: relates-to
    description: "Defines type"
---

# costs

A table. Spend per step/record; basis measured|estimated (ADR-046: measured only when read back from vendor-reported cost metadata; a rate multiplied out is estimated)

## Columns

| Column | Type | Constraints |
|---|---|---|
| `id` | TEXT | PRIMARY KEY |
| `run_id` | TEXT | NOT NULL |
| `step_id` | TEXT | NOT NULL |
| `identity_id` | TEXT |  |
| `provider` | TEXT | NOT NULL |
| `amount_usd` | REAL | NOT NULL |
| `basis` | TEXT | NOT NULL DEFAULT 'estimated' |
| `detail` | TEXT |  |
| `created_at` | TEXT | NOT NULL |

## Definition

From `spec/ledger.sql`, which is the machine-checkable form of SPEC.md §3:

```sql
CREATE TABLE costs (
  id          TEXT PRIMARY KEY,
  run_id      TEXT NOT NULL,
  step_id     TEXT NOT NULL,
  identity_id TEXT,
  provider    TEXT NOT NULL,
  amount_usd  REAL NOT NULL,              -- 0 allowed (unknown/free)
  basis       TEXT NOT NULL DEFAULT 'estimated', -- measured|estimated (ADR-046)
  detail      TEXT,                       -- JSON (credits, tokens, etc.)
  created_at  TEXT NOT NULL
);
```
