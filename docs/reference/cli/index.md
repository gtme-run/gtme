---
# generated by make docs-reference (cmd/docsgen); do not edit, fix the source and regenerate
name: "CLI"
description: "Every gtme verb with each of its forms, one table per verb, generated from gtme help --agent"
for: "You know what you want gtme to do and need the exact verb, its forms, and the exit code a script can branch on."
learn:
  - "every verb and each form it takes"
  - "the exit codes every verb shares"
  - "where each verb's own page is"
roles: [operator, builder, agent]
generated_by: "make docs-reference (cmd/docsgen): gtme help --agent, docs/_adapters.json, spec/fields/*.json, spec/ledger.sql, and each concept page's defines:. Do not edit; fix the source and regenerate."
links:
  - to: /concepts/adapter-tiers
    type: relates-to
    description: "Defines NDJSON, adapter, binding, bindings registry, fixture, and manifest"
  - to: /concepts/campaign-is-a-folder
    type: relates-to
    description: "Defines bundle and freeze"
  - to: /concepts/facts
    type: relates-to
    description: "Defines fact and provenance"
  - to: /concepts/gate-ladder
    type: relates-to
    description: "Defines dry-run, plan, and simulate"
  - to: /concepts/groups
    type: relates-to
    description: "Defines group and segment"
  - to: /concepts/identity-keys
    type: relates-to
    description: "Defines identity key"
  - to: /concepts/ledger
    type: relates-to
    description: "Defines ledger, payload, and projection"
  - to: /concepts/participants
    type: relates-to
    description: "Defines answer, participant, and pending"
  - to: /concepts/pipeline
    type: relates-to
    description: "Defines pipeline, source, and step"
  - to: /concepts/runs-and-receipts
    type: relates-to
    description: "Defines receipt, resume, and run"
  - to: /concepts/steps-and-roles
    type: relates-to
    description: "Defines compose, deliver, filter, review, role, and verify"
  - to: /concepts/types-and-traverse
    type: relates-to
    description: "Defines type"
---

# CLI

gtme has 13 verbs, in the order `gtme help --agent` prints them. Each verb's own page adds its flags, an example from the docs, and where the docs use it. Everything a verb prints for a person goes to stderr; stdout carries data, so a script or an agent can read it.

| Verb | Does |
|---|---|
| [`gtme init`](#gtme-init) | Create ~/.gtme and the ledger |
| [`gtme secret`](#gtme-secret) | Store a credential in ~/.gtme/secrets (VALUE omitted = prompt, no echo) |
| [`gtme plan`](#gtme-plan) | Resolve adapters, validate every step's needs/uses and credentials, print the plan |
| [`gtme run`](#gtme-run) | Execute a pipeline |
| [`gtme query`](#gtme-query) | read-only SQL against the ledger |
| [`gtme show`](#gtme-show) | Print the current-value projection for one identity |
| [`gtme answer`](#gtme-answer) | Record a participant's answer for one pending human/* or agent/* step |
| [`gtme runs`](#gtme-runs) | List runs, or print one run's receipt (records/cost per step) |
| [`gtme freeze`](#gtme-freeze) | Print the pipeline.yaml that produced a run, reconstructed from its stored config |
| [`gtme groups`](#gtme-groups) | List groups with their entity type and derived character (members, added/removed/touched tallies), inspect one |
| [`gtme vacuum`](#gtme-vacuum) | Evict expired payloads from the ADR-030 cache tier |
| [`gtme adapters`](#gtme-adapters) | List installed adapters with their source and pin (.source.json) |
| [`gtme help`](#gtme-help) | Print this document |

## Exit codes

Every verb exits with one of these, so a script can branch on the number.

| Code | Means |
|---|---|
| 0 | ok |
| 1 | other error |
| 2 | validation or contract error: the plan, the file, or a value was refused |
| 3 | auth or credential error |
| 4 | rate-limited by a vendor |
| 5 | network error |

## gtme init

| Form | Does |
|---|---|
| `gtme init` | create ~/.gtme and the ledger |

[Flags, an example, and where it's used](/reference/cli/init).

## gtme secret

| Form | Does |
|---|---|
| `gtme secret set KEY [VALUE]` | store a credential in ~/.gtme/secrets (VALUE omitted = prompt, no echo) |

[Flags, an example, and where it's used](/reference/cli/secret).

## gtme plan

| Form | Does |
|---|---|
| `gtme plan pipeline.yaml [--viz|--viz-only]` | resolve adapters, validate every step's needs/uses and credentials, print the plan — no network, no spend; --viz appends a diagram of the resolved plan to the listing and --viz-only prints it alone (ADR-051), both renderings of the same plan and neither changing what plan accepts or exits with |

[Flags, an example, and where it's used](/reference/cli/plan).

## gtme run

| Form | Does |
|---|---|
| `gtme run pipeline.yaml [--resume RUN_ID] [--dry-run] [--simulate]` | execute a pipeline; --resume continues a run that stopped partway; --dry-run holds deliver steps back and receipts their resolved variables instead of sending; --simulate executes everything offline from fixtures (no network, no spend, nothing persists) |

[Flags, an example, and where it's used](/reference/cli/run).

## gtme query

| Form | Does |
|---|---|
| `gtme query "SQL" [--save NAME] [--name NAME] [--list] [--format ndjson|table|csv] [--limit N]` | read-only SQL against the ledger; --save stores it as a named segment |

[Flags, an example, and where it's used](/reference/cli/query).

## gtme show

| Form | Does |
|---|---|
| `gtme show <identity-key> [--fields a,b] [--provenance]` | print the current-value projection for one identity |
| `gtme show --run RUN_ID|last [--fields a,b] [--provenance] [--limit N]` | list the records a run touched |
| `gtme show --run RUN_ID|last --pending [STEP]` | the records awaiting a participant with the surface each is shown, as text on stderr and NDJSON on stdout — what an agent reads before it answers (ADR-049) |

[Flags, an example, and where it's used](/reference/cli/show).

## gtme answer

| Form | Does |
|---|---|
| `gtme answer [RUN_ID|last|PIPELINE] [STEP] [IDENTITY_KEY] [--set field=value ...] [--as NAME] [--cost USD [--measured]] [--note TEXT]` | record a participant's answer for one pending human/* or agent/* step: a filter takes pass=true\|false and reason, a compose or review its declared fields; the value is validated against the step's declared outputs and refused naming them otherwise. Writes an `answered` event and nothing else — it never sends. STEP may be omitted when one step is pending; with no identity key and a terminal it walks the pending records. --as names the participant (default the OS user; the human/ or agent/ prefix follows the adapter), --cost records what the participant spent (estimated unless --measured), --note is free text kept with the answer (never part of a cache key) |

[Flags, an example, and where it's used](/reference/cli/answer).

## gtme runs

| Form | Does |
|---|---|
| `gtme runs [RUN_ID|last]` | list runs, or print one run's receipt (records/cost per step); a rehearsal is marked (dry) and a run that spent money and sourced nothing reads `done — 0 records, $X spent` (ADR-052, ADR-053) |

[Flags, an example, and where it's used](/reference/cli/runs).

## gtme freeze

| Form | Does |
|---|---|
| `gtme freeze [RUN_ID|last] [--bundle DIR]` | print the pipeline.yaml that produced a run, reconstructed from its stored config; --bundle assembles a portable campaign bundle instead (pipeline + referenced bindings with fixtures + registry slice + hash manifest), which `gtme run` accepts wherever it accepts a pipeline path (ADR-029) |

[Flags, an example, and where it's used](/reference/cli/freeze).

## gtme groups

| Form | Does |
|---|---|
| `gtme groups [show NAME | add NAME KEY...|--from-segment NAME|--query SQL|--type TYPE | remove NAME KEY... [--note TEXT]]` | list groups with their entity type and derived character (members, added/removed/touched tallies), inspect one (members, events, and the pipelines that wrote to and sourced from it), or hand-edit membership; snapshots evaluate a segment or SQL into extensional membership with provenance (ADR-021); a group holds one type, set at creation (ADR-054) — --type sets an untyped legacy group's type once, and is required when a key is ambiguous or no key is given; --note records a removal's reason (ADR-032) |

[Flags, an example, and where it's used](/reference/cli/groups).

## gtme vacuum

| Form | Does |
|---|---|
| `gtme vacuum` | evict expired payloads from the ADR-030 cache tier — and nothing else; facts are append-only forever (SPEC §8) |

[Flags, an example, and where it's used](/reference/cli/vacuum).

## gtme adapters

| Form | Does |
|---|---|
| `gtme adapters` | list installed adapters with their source and pin (.source.json) |
| `gtme adapters search TEXT` | search the bindings registry index by id, vendor, description and role (GTME_REGISTRY overrides the index URL) |
| `gtme adapters add github.com/<owner>/<repo>/<path>[@ref]` | fetch a binding at a pinned ref, verify it (schema + fixtures offline; nothing installs unverified), install it under ~/.gtme/adapters/ with .source.json beside it |
| `gtme adapters verify ID` | validate an installed binding against the schema, run its fixtures offline, print the hosts it will call and the credentials it will demand |
| `gtme adapters update ID [@ref]` | re-fetch at a newer ref — the only thing that moves a pin |

[Flags, an example, and where it's used](/reference/cli/adapters).

## gtme help

| Form | Does |
|---|---|
| `gtme help --agent` | print this document |
| `gtme help --bindings` | print the binding contract — schema, discovery path, a reference binding, the fixtures expectation — for authoring an adapter this binary does not ship (see `bindings` below) |

[Flags, an example, and where it's used](/reference/cli/help).
