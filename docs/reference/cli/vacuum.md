---
# generated by make docs-reference (cmd/docsgen); do not edit, fix the source and regenerate
name: "gtme vacuum"
description: "Evict expired payloads from the ADR-030 cache tier"
for: "You're about to type `gtme vacuum` and want its forms, its flags, and the exit codes it can return."
learn:
  - "every form of `gtme vacuum` and what each does"
  - "each flag and what it changes"
  - "the exit codes a script can rely on"
  - "where the docs use it"
roles: [operator, builder, agent]
generated_by: "make docs-reference (cmd/docsgen): gtme help --agent, docs/_adapters.json, spec/fields/*.json, spec/ledger.sql, and each concept page's defines:. Do not edit; fix the source and regenerate."
links:
  - to: /concepts/facts
    type: relates-to
    description: "Defines fact and provenance"
  - to: /concepts/gate-ladder
    type: relates-to
    description: "Defines plan"
  - to: /concepts/ledger
    type: relates-to
    description: "Defines payload"
---

# gtme vacuum

Evict expired payloads from the ADR-030 cache tier — and nothing else; facts are append-only forever (SPEC §8)

## Forms

```
gtme vacuum
```

## Exit codes

| Code | Means |
|---|---|
| 0 | ok |
| 1 | other error |
| 2 | validation or contract error: the plan, the file, or a value was refused |
| 3 | auth or credential error |
| 4 | rate-limited by a vendor |
| 5 | network error |

## Example

The first form with every optional part left out:

```
gtme vacuum
```

## Used in

- [Facts have provenance](/concepts/facts): The command that deletes expired payloads and nothing else
