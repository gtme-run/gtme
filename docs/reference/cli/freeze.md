---
# generated by make docs-reference (cmd/docsgen); do not edit, fix the source and regenerate
name: "gtme freeze"
description: "Print the pipeline.yaml that produced a run, reconstructed from its stored config"
for: "You're about to type `gtme freeze` and want its forms and its flags."
learn:
  - "every form of `gtme freeze` and what each does"
  - "each flag and what it changes"
  - "where the docs use it"
roles: [operator, builder, agent]
generated_by: "make docs-reference (cmd/docsgen): gtme help --agent, docs/_adapters.json, spec/fields/*.json, spec/ledger.sql, and each concept page's defines:. Do not edit; fix the source and regenerate."
links:
  - to: /concepts/adapter-tiers
    type: relates-to
    description: "Defines binding, fixture, and manifest"
  - to: /concepts/campaign-is-a-folder
    type: relates-to
    description: "Defines bundle and freeze"
  - to: /concepts/pipeline
    type: relates-to
    description: "Defines pipeline"
  - to: /concepts/runs-and-receipts
    type: relates-to
    description: "Defines run"
---

# gtme freeze

Print the pipeline.yaml that produced a run, reconstructed from its stored config; --bundle assembles a portable campaign bundle instead (pipeline + referenced bindings with fixtures + registry slice + hash manifest), which `gtme run` accepts wherever it accepts a pipeline path (ADR-029)

## Forms

```
gtme freeze [RUN_ID|last] [--bundle DIR]
```

## Flags

| Flag | Takes | What it does |
|---|---|---|
| `--bundle` | `DIR` | --bundle assembles a portable campaign bundle instead (pipeline + referenced bindings with fixtures + registry slice + hash manifest), which `gtme run` accepts wherever it accepts a pipeline path (ADR-029) |

It exits with one of the [exit codes](/reference/cli#exit-codes) every verb shares.

## Example

The first form with every optional part left out:

```
gtme freeze
```

## Used in

- [A campaign is a folder](/concepts/campaign-is-a-folder): The command that writes a bundle, or prints a pipeline's YAML without one
