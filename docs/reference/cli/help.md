---
# generated by make docs-reference (cmd/docsgen); do not edit, fix the source and regenerate
name: "gtme help"
description: "Print this document"
for: "You're about to type `gtme help` and want its forms, its flags, and the exit codes it can return."
learn:
  - "every form of `gtme help` and what each does"
  - "each flag and what it changes"
  - "the exit codes a script can rely on"
  - "where the docs use it"
roles: [operator, builder, agent]
generated_by: "make docs-reference (cmd/docsgen): gtme help --agent, docs/_adapters.json, spec/fields/*.json, spec/ledger.sql, and each concept page's defines:. Do not edit; fix the source and regenerate."
links:
  - to: /concepts/adapter-tiers
    type: relates-to
    description: "Defines adapter, binding, and fixture"
  - to: /concepts/gate-ladder
    type: relates-to
    description: "Defines plan"
---

# gtme help

Print this document.

## Forms

```
gtme help --agent
gtme help --bindings
```

| Form | Does |
|---|---|
| `gtme help --agent` | print this document |
| `gtme help --bindings` | print the binding contract — schema, discovery path, a reference binding, the fixtures expectation — for authoring an adapter this binary does not ship (see `bindings` below) |

## Flags

| Flag | Takes | Form | What it does |
|---|---|---|---|
| `--agent` | nothing | `gtme help --agent` | See the form. |
| `--bindings` | nothing | `gtme help --bindings` | See the form. |

## Exit codes

| Code | Means |
|---|---|
| 0 | ok |
| 1 | other error |
| 2 | validation or contract error: the plan, the file, or a value was refused |
| 3 | auth or credential error |
| 4 | rate-limited by a vendor |
| 5 | network error |

## Example

From [Add a vendor](/start/add-a-vendor):

```sh
gtme help --bindings > bindings.json
```

## Used in

- [For agents](/start/for-agents): The help verb, including the --agent output the agent reads
